# HINTS

## Load order

    zsh                          sh

    /etc/zshenv                  /etc/profile
    ~/.zshenv                    ~/.profile
    /etc/zprofile (login)
    ~/.zprofile   (login)
    /etc/zshrc    (iact)
    ~/.zshrc      (iact)
    /etc/login    (login)
    ~/.zlogin     (login)


    /etc/zlogout  (exit)
    ~/.zlogout    (exit)

